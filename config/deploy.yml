# Name of your application. Used to uniquely configure containers.
service: microblog
# ubuntu-s-1vcpu-1gb-35gb-intel-lon1-01-microblog-backend
# Name of the container image (use your-user/app-name on external registries).
image: davidslvuk/microblog-backend

# Deploy to these servers.
servers:
  web:
    hosts:
      - <%= ENV.fetch("BACKEND_HOST", "206.189.19.73") %>
    # options:
    #   "add-host": host.docker_hostname + ":host-gateway"
  # job:
  #   hosts:
  #     - <%= ENV.fetch("BACKEND_HOST", "api.example.com") %>
  #   cmd: bin/jobs

# Enable SSL auto certification via Let's Encrypt and allow for multiple apps on a single web server.
# If used with Cloudflare, set encryption mode in SSL/TLS setting to "Full" to enable CF-to-app encryption.
#
# Using an SSL proxy like this requires turning on config.assume_ssl and config.force_ssl in production.rb!
#
# Don't use this when deploying to multiple web servers (then you have to terminate SSL at your load balancer).
proxy:
  ssl: true
  host: <%= ENV.fetch("BACKEND_HOST", "microblog-be.davidslv.uk") %>

# Where you keep your container images.
registry:
  # Alternatives: hub.docker.com / registry.digitalocean.com / ghcr.io / ...
  server: <%= ENV.fetch("DOCKER_REGISTRY", "docker.io") %>

  # Needed for authenticated registries.
  username: <%= ENV.fetch("DOCKER_USERNAME", "davidslvuk") %>

  # Always use an access token rather than real password when possible.
  password:
    - KAMAL_REGISTRY_PASSWORD

# Inject ENV variables into containers (secrets come from .kamal/secrets).
env:
  secret:
    - RAILS_MASTER_KEY
    - SECRET_KEY_BASE
  clear:
    RAILS_ENV: production
    # Run the Solid Queue Supervisor inside the web server's Puma process to do jobs.
    # When you start using multiple servers, you should split out job processing to a dedicated machine.
    SOLID_QUEUE_IN_PUMA: "true"
    # Temporarily set DATABASE_URL as clear to debug connection issue
    # TODO: Move back to secrets once issue is resolved
    DATABASE_URL: postgresql://doadmin:<database-password-from-secrets>@private-db-postgresql-lon1-11936-do-user-28554529-0.j.db.ondigitalocean.com:25060/defaultdb?sslmode=require

    # Set number of processes dedicated to Solid Queue (default: 1)
    # JOB_CONCURRENCY: 3

    # Set number of cores available to the application on each server (default: 1).
    # WEB_CONCURRENCY: 2

    # Database connection is configured via DATABASE_URL secret (see .kamal/secrets)
    # DATABASE_HOST, DATABASE_PORT, and DATABASE_PASSWORD are needed for replica, cache, queue, and cable databases
    # which don't use DATABASE_URL directly
    DATABASE_HOST: private-db-postgresql-lon1-11936-do-user-28554529-0.j.db.ondigitalocean.com
    DATABASE_PORT: 25060
    DATABASE_USERNAME: doadmin
    DATABASE_PASSWORD: <database-password-from-secrets>
    
    # Cache, queue, and cable databases use the same host/port and credentials as primary
    # Using doadmin user for all databases (can create separate users later if needed)
    CACHE_DB_HOST: private-db-postgresql-lon1-11936-do-user-28554529-0.j.db.ondigitalocean.com
    CACHE_DB_PORT: 25060
    CACHE_DB_USERNAME: doadmin
    CACHE_DB_PASSWORD: <database-password-from-secrets>
    QUEUE_DB_HOST: private-db-postgresql-lon1-11936-do-user-28554529-0.j.db.ondigitalocean.com
    QUEUE_DB_PORT: 25060
    QUEUE_DB_USERNAME: doadmin
    QUEUE_DB_PASSWORD: <database-password-from-secrets>
    CABLE_DB_HOST: private-db-postgresql-lon1-11936-do-user-28554529-0.j.db.ondigitalocean.com
    CABLE_DB_PORT: 25060
    CABLE_DB_USERNAME: doadmin
    CABLE_DB_PASSWORD: <database-password-from-secrets>

    # Log everything from Rails
    # RAILS_LOG_LEVEL: debug

# Aliases are triggered with "bin/kamal <alias>". You can overwrite arguments on invocation:
# "bin/kamal logs -r job" will tail logs from the first server in the job section.
aliases:
  console: app exec --interactive --reuse "bin/rails console"
  shell: app exec --interactive --reuse "bash"
  logs: app logs -f
  dbc: app exec --interactive --reuse "bin/rails dbconsole --include-password"

# Use a persistent storage volume for sqlite database files and local Active Storage files.
# Recommended to change this to a mounted volume path that is backed up off server.
volumes:
  - "volume_lon1_01:/rails/storage"

# Bridge fingerprinted assets, like JS and CSS, between versions to avoid
# hitting 404 on in-flight requests. Combines all files from new and old
# version inside the asset_path.
asset_path: /rails/public/assets

# # Health check
# healthcheck:
#   path: /up
#   port: 80
#   max_attempts: 10
#   interval: 10s

# # Resource limits
# limits:
#   memory: 2g
#   cpus: 2


# Configure the image builder.
builder:
  arch: amd64

  # # Build image via remote server (useful for faster amd64 builds on arm64 computers)
  # remote: ssh://docker@docker-builder-server
  #
  # # Pass arguments and secrets to the Docker build process
  # args:
  #   RUBY_VERSION: 3.4.7
  # secrets:
  #   - GITHUB_TOKEN
  #   - RAILS_MASTER_KEY

# Use a different ssh user than root
# ssh:
#   user: app

# Use accessory services (secrets come from .kamal/secrets).
# accessories:
#   db:
#     image: mysql:8.0
#     host: 192.168.0.2
#     # Change to 3306 to expose port to the world instead of just local network.
#     port: "127.0.0.1:3306:3306"
#     env:
#       clear:
#         MYSQL_ROOT_HOST: '%'
#       secret:
#         - MYSQL_ROOT_PASSWORD
#     files:
#       - config/mysql/production.cnf:/etc/mysql/my.cnf
#       - db/production.sql:/docker-entrypoint-initdb.d/setup.sql
#     directories:
#       - data:/var/lib/mysql
#   redis:
#     image: valkey/valkey:8
#     host: 192.168.0.2
#     port: 6379
#     directories:
#       - data:/data
